export default Object.freeze({
    fileName: 'ModelSubIds.d.ts',
    content: `type ModelSubIds = {
    groupIds: string[]
    entityIds: string[]
    mappedSuperClassIds: string[]
    embeddableTypeIds: string[]
    enumerationIds: string[]
    associationIds: string[]
}`,
})

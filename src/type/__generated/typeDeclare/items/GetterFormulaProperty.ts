export default Object.freeze({
    fileName: 'GetterFormulaProperty.d.ts',
    content: `type GetterFormulaProperty = {
    category: "FORMULA_GETTER"
    dependencies: string[]
    body: string
    rawType: string
} & BaseProperty`,
})

export default Object.freeze({
    fileName: 'Group.d.ts',
    content: `type Group = {
    id: string
    name: string
    comment: string
    color: string
    basePackagePath: string
    baseTableSchema: string
}`,
})

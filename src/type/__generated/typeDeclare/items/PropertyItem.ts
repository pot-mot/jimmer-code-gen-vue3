export default Object.freeze({
    fileName: 'PropertyItem.d.ts',
    content: `type PropertyItem = {
    name: string
    comment: string
    type: string
    annotations: string[]
    body?: string
}`,
})
